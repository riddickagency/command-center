{
  "name": "command-center",
  "version": "1.0.0",
  "private": true,
  "description": "Lawrence Riddick's cross-device life + FPS command center, hosted on Vercel with Upstash Redis (free tier) as the data store.",
  "dependencies": {
    "@upstash/redis": "^1.31.6"
  }
}
